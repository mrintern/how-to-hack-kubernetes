loose instructions to launch kube_security_lab

Requirements:
- (windows) ubuntu app
- kind
- docker
- ansible
- kubectl


0. (Windows) install ubuntu app from Microsoft store

1. install kind, docker, ansible, kubectl...

* note to self, consider writing a script for this...

  INSTALL docker
  ...

  INSTALL kubectl
  ...

  INSTALL ansible
  - sudo apt-get update
  - sudo apt-get install software-properties-common
  - sudo apt-add-repository ppa:ansible/ansible
  - sudo apt-get update
  - sudo apt-get install ansible

  INSTALL kind
  - curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.14.0/kind-linux-amd64
  - chmod +x ./kind
  - sudo mv ./kind /usr/bin/kind

  INSTALL pip3
  - sudo apt install python3-pip

  INSTALL python3 docker module
  - pip3 install docker

2. verify installation with the following commands
- kubectl
- kind
- docker
- ansible

3. create kind cluster
- kind cluster create

4. clone project
- git clone https://github.com/raesene/kube_security_lab.git
- cd kube_security_lab

5. ansible-playbook insecure-port.yml
 If everything works thus far, I think we're ready to start hacking.


 "failed to init node with kubeadm"
 docker exec --privileged insecureport-control-plane
 kubeadm init --skip-phases=preflight --config=/kind/kubeadm.conf
 --skip-token-print --v=6

INSTALL DEPENDS ON kubeadm
MUST INSTALL kubeadm...

https://kubernetes.io/docs/setup/production-environment/tools/kubeadm/create-cluster-kubeadm/
